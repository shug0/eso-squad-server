{
  "name": "eso-squad",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "fix": "standard --fix",
    "dev": "concurrently \"yarn run client_start\" \"npm run server_dev\""
  },
  "devDependencies": {
    "concurrently": "^4.1.0"
  },
  "standard": {
    "parser": "babel-eslint"
  }
}
